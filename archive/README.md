# Archived website export

The `archive/site` directory contains deprecated assets from the previous
website export workflow (static site). This path is archived
for historical reference only and is not part of the current pipeline or
visualization workflow.

The new default visualization is the Shiny app under `shiny/`.
